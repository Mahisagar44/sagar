#include <stdio.h>

int main(void)
{
    int NumberOne, NumberTwo, Sum;
    printf("Enter the first number:");
    scanf("%d", &NumberOne);
    printf("Enter the second number:");
    scanf("%d", &NumberTwo);
    Sum = NumberOne + NumberTwo;
    printf("The sum of %d and %d is %d", NumberOneSum);
}